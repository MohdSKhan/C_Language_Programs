#include<stdio.h>
#include<conio.h>
int sum(int n);
void main()
{
    int n,k;
    scanf("%d",&n);
    k=sum(n);
    printf("%d",k);


}
int sum(int n)
{
    if(n==0)
        return 0;
    return(sum(n/10)+n%10);

}


